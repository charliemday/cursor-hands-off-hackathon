# From `modal deploy modal/vllm_inference.py` output (NOT the dashboard link)
# Example: https://your-workspace--merch-vllm-serve.modal.run
MODAL_INFERENCE_URL=

# From `modal deploy modal/vllm_vision.py` output — used for photo upload search
# Example: https://your-workspace--merch-vllm-vision-serve.modal.run
MODAL_VISION_URL=

FIRECRAWL_API_KEY=

# Supabase — from your project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
